"use client"

import styled from "styled-components";

export const MainSearch = styled.section`
   display: flex;
   flex-direction: column;   
   align-items: center;
   gap: 0;
   margin: 0 auto;
   
`;
